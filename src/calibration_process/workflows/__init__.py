# -*- coding:utf-8 -*-
"""Explicit scientific workflow stages.

This package holds the reusable stages and, under ``versions``, the explicit
per-version workflows.  It reuses the protected scientific kernel
(``file_lib``, ``util_lib``, ``fitting``, ``lib_plot``) directly and only
changes the orchestration.
"""
