[project]
name = "grid-common"
version = "0.1.0"
description = "Shared numerics for GRID calibration (spectrum, resolution, naming)"
authors = [
    { name = "wqd", email = "wqd.2019@tsinghua.org.cn" }
]
dependencies = [
    "numpy>=1.24.4",
]
requires-python = ">= 3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
